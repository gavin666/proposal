spring:
  profiles:
    active: prod

  datasource:
    url: jdbc:mysql://127.0.0.1:3306/test?autoReconnect=true
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: root

  jpa:
      hibernate:
        ddl-auto: update
      show-sql: true

logging:
  level: debug
  file: D:/test.log
